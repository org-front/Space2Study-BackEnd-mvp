@AGENTS.md

You are Claude Code in this repo. There is no Cursor Browser and no Bugbot. For Swagger UI and HTTP (CORS, 401, JSON bodies, cookies) use the `chrome-devtools` MCP against `http://localhost:8080`. On first run, approve project MCP servers from `.mcp.json` (`claude` interactive session). Do not add Playwright, Knip, CodeRabbit, or MUI MCP. Never put tokens in committed files.
